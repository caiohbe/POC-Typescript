function convertRealToIene(req, res) {
    var real = req.query;
    var iene = 0.04;
    //const conversion = real/iene
    res.send({
    //resultado: `A quantidade de ${real} reais em ienes é equivalente a ${conversion} ienes`
    });
}
export {};
